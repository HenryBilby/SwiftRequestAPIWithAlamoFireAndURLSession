# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'

target 'RequestAPIWithAlamoFireAndURLSession' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RequestAPIWithAlamoFireAndURLSession
	pod 'Alamofire'
	pod 'SwiftyJSON'

  target 'RequestAPIWithAlamoFireAndURLSessionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RequestAPIWithAlamoFireAndURLSessionUITests' do
    # Pods for testing
  end

end
